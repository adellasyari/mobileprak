import 'package:flutter/material.dart';

class AppConstants {
  // Jarak padding umum
  static const double paddingMedium = 16.0;

  // Daftar gradien dashboard. Setiap item adalah List<Color> yang digunakan sebagai gradientColors.
  static const List<List<Color>> dashboardGradients = [
    [Color(0xFF6DD5FA), Color(0xFF2980B9)],
    [Color(0xFFF6D365), Color(0xFFFDA085)],
    [Color(0xFF9BFAE0), Color(0xFF3AA3FF)],
    [Color(0xFFFBC2EB), Color(0xFFA6C1EE)],
  ];
}
